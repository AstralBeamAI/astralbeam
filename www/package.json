{
  "name": "www",
  "private": true,
  "description": "https://www.astralbeam.ai/",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 3001",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port 4001",
    "check": "astro check",
    "test": "astro build && vitest run",
    "deploy": "deno task check && deno task test && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "@fontsource-variable/space-grotesk": "^5.2.8",
    "@fontsource/anton": "^5.2.8",
    "@fontsource/jetbrains-mono": "^5.2.8",
    "astro": "7.1.1",
    "sharp": "^0.35.2"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "@types/node": "^22.10.2",
    "typescript": "^6.0.2",
    "vitest": "4.1.10",
    "wrangler": "^4.112.0"
  }
}
