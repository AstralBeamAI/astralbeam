{
  "name": "AstralBeam",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "./scripts/setup.sh",
  "start": "service docker start && docker compose up -d --wait",
  "ports": [
    {
      "name": "Product application",
      "port": 4500
    },
    {
      "name": "Public website",
      "port": 4600
    }
  ],
  "terminals": [
    {
      "name": "Product application",
      "command": "cd webapp && deno task dev",
      "description": "Runs the TanStack Start application on port 4500."
    },
    {
      "name": "Public website",
      "command": "cd www && deno task dev",
      "description": "Runs the Astro website on port 4600."
    }
  ]
}
